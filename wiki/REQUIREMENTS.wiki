#summary One-sentence summary of this page.

= Requirements =
 * ruby
 * ruby additional libs lib: stomp (with rubygems installed, just do 'gems install stomp'
 * a JMS broker like [http:://activemq.apache.org apache activemq] or fuse message broker: in this case you need also Java jdk 1.5